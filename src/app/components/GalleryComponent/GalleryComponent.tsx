import type { NextComponentType, NextPageContext } from "next";

interface Props {}

const GalleryComponent: NextComponentType<NextPageContext, {}, Props> = (
	props: Props,
) => {
	return (
		<div></div>
	)
}

export default GalleryComponent